# =============================================================================
# Rehabify Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# SECURITY NOTES:
# - Variables prefixed with NEXT_PUBLIC_* are exposed to the browser
# - All other variables are server-only (never bundled in client code)
# - Never commit .env.local or any file with actual secrets
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Neon PostgreSQL)
# Get credentials from: https://console.neon.tech
# -----------------------------------------------------------------------------

# [SERVER ONLY] Neon PostgreSQL connection string with pooler
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/rehabify?sslmode=require

# [SERVER ONLY] Neon API key for database management operations
NEON_API_KEY=your-neon-api-key

# -----------------------------------------------------------------------------
# Voice AI (Vapi)
# Get credentials from: https://dashboard.vapi.ai
# -----------------------------------------------------------------------------

# [CLIENT SAFE] Vapi public key for browser SDK initialization
NEXT_PUBLIC_VAPI_PUBLIC_KEY=your-vapi-public-key

# [SERVER ONLY] Vapi private key for server-side API calls
VAPI_PRIVATE_KEY=your-vapi-private-key

# [SERVER ONLY] Secret for validating Vapi webhook signatures
VAPI_WEBHOOK_SECRET=your-vapi-webhook-secret

# -----------------------------------------------------------------------------
# AI Plan Generation (Google Gemini)
# Get credentials from: https://aistudio.google.com/apikey
# -----------------------------------------------------------------------------

# [SERVER ONLY] Google Gemini API key for AI-powered plan generation
GEMINI_API_KEY=your-gemini-api-key

# [SERVER ONLY] Gemini model identifier (default: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# [CLIENT SAFE] Your app's deployed URL (used for OAuth callbacks, links)
# Development: http://localhost:3000
# Staging: https://staging.rehabify.app
# Production: https://rehabify.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Feature Flags
# All feature flags accept "true" or "false" (as strings)
# -----------------------------------------------------------------------------

# [CLIENT SAFE] Enable voice AI coaching features (default: true)
NEXT_PUBLIC_ENABLE_VOICE=true

# [CLIENT SAFE] Enable analytics/telemetry tracking (default: false)
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# [CLIENT SAFE] Enable demo mode - skips auth for testing (default: false)
# WARNING: Never enable in production!
NEXT_PUBLIC_DEMO_MODE=true

# -----------------------------------------------------------------------------
# OAuth Providers (Optional)
# Only required if you want Google Sign-In
# Configure at: https://console.cloud.google.com/apis/credentials
# -----------------------------------------------------------------------------

# [SERVER ONLY] Google OAuth client ID
GOOGLE_CLIENT_ID=

# [SERVER ONLY] Google OAuth client secret
GOOGLE_CLIENT_SECRET=

# =============================================================================
# Environment-Specific Settings
# =============================================================================
#
# DEVELOPMENT (local):
#   - NEXT_PUBLIC_APP_URL=http://localhost:3000
#   - NEXT_PUBLIC_DEMO_MODE=true (optional, for testing without auth)
#   - Use development/staging API keys
#
# STAGING:
#   - NEXT_PUBLIC_APP_URL=https://staging.rehabify.app
#   - NEXT_PUBLIC_DEMO_MODE=false
#   - Use staging database and API keys
#
# PRODUCTION:
#   - NEXT_PUBLIC_APP_URL=https://rehabify.app
#   - NEXT_PUBLIC_DEMO_MODE=false (CRITICAL!)
#   - NEXT_PUBLIC_ENABLE_ANALYTICS=true
#   - Use production database and API keys
#   - All secrets should be rotated and unique to production
#
# =============================================================================
